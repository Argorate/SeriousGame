using System;

namespace AssemblyCSharp
{
	/* Enum qui qui permet d'identifier les differnets types de cartes
	 * Ce n'est pas le mécanisme le plus fin, mais c'est simple à mettre en oeuvre pour un petit projet
	 * (et c'est plus pratique avec les RPC…)
	 */
	
	public enum CartesEnum
	{
		ConstruireDesCentralesNucleaire,
		ConstruireDesEoliennes,	
	}
	
}

